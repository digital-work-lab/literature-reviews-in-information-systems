---
title: "Statistics"
format:
  html:
    toc: true
    toc-depth: 2
execute:
  echo: true
  warning: false
  message: false
---

```{r}
#| label: setup
#| include: false
library(dplyr)
library(ggplot2)
library(readr)
library(plotly)
library(DT)

# Load your dataset
# Replace with your actual file
df <- readr::read_csv("records.csv", show_col_types = FALSE)
```

## Timeline

```{r}
#| label: pubs_over_time
#| fig-cap: "Number of papers over time"

df_time <- df %>%
  dplyr::count(year) %>%
  dplyr::arrange(year)

plot_ly(
  data = df_time,
  x = ~year,
  y = ~n,
  type = "scatter",
  mode = "lines+markers"
) %>%
  layout(
    xaxis = list(title = "Year"),
    yaxis = list(title = "Number of Publications")
  )
```

## Journals

```{r}
#| label: journal_freq
#| tbl-cap: "Frequency of journals"

df %>%
  dplyr::count(journal, sort = TRUE) %>%
  DT::datatable(
    options = list(pageLength = 10),
    rownames = FALSE
  )
```

## Types of reviews

```{r}
#| label: lr_type_pare_et_al
#| fig-cap: "Distribution of literature review types (Paré et al.)"

df_lr_type <- df %>%
  dplyr::count(lr_type_pare_et_al, sort = TRUE)

plot_ly(
  data = df_lr_type,
  x = ~lr_type_pare_et_al,
  y = ~n,
  type = "bar"
) %>%
  layout(
    xaxis = list(title = "Literature review type"),
    yaxis = list(title = "Count")
  )
```
